- hosts: "{{ target_server }}"
  gather_facts: false
  tasks:
    - name: Test connection
      ping:
      
    - name: Show hostname
      debug:
        msg: "Connected to {{ inventory_hostname }}"
        
    - name: Show target server info
      debug:
        msg: "Target server: {{ target_server }}, Role: {{ role }}"
        
    - name: Test successful completion
      debug:
        msg: "Minimal test completed successfully!"
